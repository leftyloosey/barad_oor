// import { useState } from 'react'
// import { useQuery } from '@apollo/client'
// import { useQuery } from '@apollo/client'

// import './App.css'

function App() {
  // const { data, loading, error } = useQuery(THE_QUERY, {})
  // if (loading) return <p>Loading</p>
  // if (error) return <p>Something Went Wrong</p>

  return <>App. Sup.</>
}

export default App
